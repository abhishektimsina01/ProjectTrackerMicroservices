// generateAccessAndRefreshToken and generateAccessToken
import jwt from "jsonwebtoken"

const generateAccessAndRefreshToken = (user_data) => {
    // we sign the json web token here
}

const generateAccessToken = (user_data) => {

}

export {generateAccessAndRefreshToken, generateAccessToken}